#include <stdio.h>

int sub(int n1,int n2);
int soma(int n1,int n2);
int div(int n1,int n2);
int multi(int n1,int n2);
int main()
{
    do
    {
    int escolha, num1, num2;
    printf("Digite o (1) para fazer uma multiplicação: ");
    printf("Digite o (2) para fazer uma divisão: ");
    printf("Digite o qualquer outro número para parar o programa: ");
    scanf("%i", &escolha);
    if (escolha == 1)
    {
        printf("Digite o número a ser multiplicado: ");
        
    }
    
    while (escolha = 1 || escolha = 2);
    }
}